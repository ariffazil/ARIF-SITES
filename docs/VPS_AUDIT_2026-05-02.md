# VPS Live Architecture Audit — 2026-05-02

> **Ditempa Bukan Diberi** — 999 SEAL ALIVE
> **CLAIM:** No destructive actions taken. All proposed operations are 888_HOLD.

---

## Summary (5 bullets)

- **11 containers running** on VPS: arifOS MCP, A-FORGE bridge, GEOX, WEALTH, WELL, AAA A2A, Hermes ASI, Postgres, Caddy, GEOX GUI, BuildKit — all healthy or running.
- **Caddyfile drift fixed** (arifOS/Caddyfile): `arifosmcp.arif-fazil.com` now redirects to `mcp.arif-fazil.com`, no hard-coded IPs remain, all routes use service names. Validated ✅.
- **VAULT999 health unreachable** is expected: `vault999` and `vault999-writer` containers are not running — arifOS probes `http://vault999:8100/health`, not Postgres directly.
- **Compose/live topology split:** Live services (arifosmcp, geox_eic, etc.) are NOT under compose project `compose`; they run as standalone containers or under different compose projects. Compose file describes intent, not live state.
- **No `docker-compose.yml` at `/root/arifOS/`** — postgres label references a non-existent file; Postgres is a protected stateful service, not to be recreated casually.

---

## 1. Live Container State

```
> ⚠️ **HISTORICAL** — Ports below are stale. Live VPS: arifOS **8088**, GEOX **18081**, WEALTH **18082**. WELL is dead.

RUNNING (11):
  arifosmcp         ghcr.io/ariffazil/arifos:latest    127.0.0.1:8088     healthy ✅
  caddy             caddy:2-alpine                     0.0.0.0:80/443     healthy ✅
  geox_eic          geox-geox:latest                  127.0.0.1:18081    healthy ✅
  geox_gui          nginx:alpine                      internal 80        running
  postgres          postgres:16-alpine                 internal 5432      healthy ✅
  wealth-organ      compose-wealth-organ:v1.0.0        127.0.0.1:18082    healthy ✅
  well              ghcr.io/ariffazil/well:latest      127.0.0.1:8083     dead ❌
  af-bridge-prod    a-forge-af-bridge                 127.0.0.1:7071     healthy ✅
  aaa-a2a           aaa-a2a:v1.0.0                   127.0.0.1:3001     running
  hermes-agent      hermes-agent:v1.0.0               127.0.0.1:3002     running
  buildx_buildkit   moby/buildkit:buildx-stable-1      none               running

STOPPED (declared in compose, not running):
  vault999          compose-vault999:v1.0.0            127.0.0.1:8100     not running
  vault999-writer   compose-vault999-writer:v1.0.0     127.0.0.1:5001     not running
  redis             redis:7-alpine                     127.0.0.1:6379     not running
  qdrant            qdrant/qdrant:latest               127.0.0.1:6333     not running
  ollama-engine-prod ollama/ollama:latest              127.0.0.1:11434    not running
  dozzle            amir20/dozzle:v1.0.0               :8888              not running
  forge-notifier    compose-forge-notifier:v1.0.0       none               not running
```

---

## 2. Caddyfile Ingress Map (arifOS/Caddyfile)

```
arif-fazil.com              → /var/www/html/arif/dist         (static)
arifos.arif-fazil.com      → /var/www/html/arifos           (static + MCP routes)
aaa.arif-fazil.com         → /var/www/html/aaa              (static + A2A routes)
geox.arif-fazil.com        → /var/www/html/geox + geox_eic  (static + MCP)
wealth.arif-fazil.com      → /var/www/html/wealth + wealth-organ
well.arif-fazil.com        → well:8083 + root redirect
mcp.arif-fazil.com         → arifosmcp:8080 (canonical MCP API)
arifosmcp.arif-fazil.com   → 302 → mcp.arif-fazil.com        (legacy redirect)
apex.arif-fazil.com        → 301 → arif-fazil.com/apex
wiki.arif-fazil.com        → 301 → arifos.arif-fazil.com/wiki
dozzle.arif-fazil.com      → dozzle:8080 (not running — orphan route)
ollama.arif-fazil.com      → ollama-engine-prod:11434 (not running — orphan route)
```

### Caddyfile Changes Applied (reversible)

| Change | Before | After |
|--------|--------|-------|
| `arifosmcp.arif-fazil.com` | `import aasic-landing arifosmcp` (static landing) | `redir https://mcp.arif-fazil.com{uri} permanent` |
| `mcp.arif-fazil.com` routes | Only `/mcp*` proxied; rest fell through to static → redirect loop | `/mcp*`, `/health`, `/tools`, `/tools.json`, `/webmcp*`, `/a2a*`, `/.well-known/agent.json` all proxied to `arifosmcp:8080` |
| Hard-coded IPs | `172.19.0.x` in reverse_proxy directives | Service names: `arifosmcp`, `geox_eic`, `wealth-organ`, `well` |
| `well.arif-fazil.com` root | `redir https://arifos.arif-fazil.com` | Preserved (root still redirects) |

**Validation:** `docker exec caddy caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile` → `Valid configuration` ✅

**888_HOLD — Caddy reload (not executed):**
```
docker exec caddy caddy reload --config /etc/caddy/Caddyfile
Risk: brief misroute if upstream DNS not yet resolved
Rollback: restore previous Caddyfile, reload again
```

---

## 3. VAULT999 Wiring

| Component | Status | Health probe target |
|-----------|--------|-------------------|
| `postgres` | Running ✅ | TCP `postgres:5432` |
| `arifosmcp` | Running ✅ | Probes `http://vault999:8100/health` for `vault999_health` |
| `vault999` | **Not running** | Expected at `http://vault999:8100/health` |
| `vault999-writer` | **Not running** | Expected at `http://vault999-writer:5001` |
| `aaa-a2a` | Running | Reports `vault: DISCONNECTED` — consistent with missing writer |

**Conclusion:** `vault999_health: unreachable` in arifOS `/health` is **expected and explained** — the `vault999` HTTP container is not running. Postgres is healthy and serving as the database backend. The issue is the `vault999` service endpoint, not Postgres storage.

**888_HOLD — vault999 start (not executed):**
```
docker compose -p compose -f /root/compose/docker-compose.yml up -d vault999 vault999-writer
Risk: writes to VAULT999 tables, schema may need migration
Rollback: docker compose -p compose stop vault999 vault999-writer
Prerequisite: verify images, secrets, schema, arifos_core_network membership
```

---

## 4. Compose vs Live Topology

**Key finding:** Live containers (arifosmcp, geox_eic, postgres, etc.) are **not** under compose project `compose`. They run as standalone containers or under different compose projects. `/root/compose/docker-compose.yml` describes **intended state**, not live state.

| Service | Live? | Compose project | Notes |
|---------|-------|---------------|-------|
| `arifosmcp` | yes | standalone (direct run) | healthy, 13 tools |
| `caddy` | yes | standalone | uses `/root/arifOS/Caddyfile` |
| `geox_eic` | yes | standalone | healthy |
| `postgres` | yes | standalone (was `arifos` project) | labeled for missing `/root/arifOS/docker-compose.yml` |
| `wealth-organ` | yes | `compose` project | healthy |
| `well` | yes | standalone | healthy |
| `af-bridge-prod` | yes | standalone | healthy |
| `aaa-a2a` | yes | standalone | running |
| `hermes-agent` | yes | standalone | running |
| `geox_gui` | yes | standalone | running |
| `vault999` | no | `compose` | planned/later |
| `vault999-writer` | no | `compose` | planned/later |
| `redis` | no | `compose` | optional |
| `qdrant` | no | `compose` | optional |
| `ollama` | no | `compose` | optional |
| `dozzle` | no | `compose` | orphan route in Caddy |

**Recommendation:** Split `docker-compose.yml` into `docker-compose.active.yml` (caddy, wealth-organ) and `docker-compose.optional.yml` (redis, qdrant, ollama, vault999, vault999-writer, dozzle, forge-notifier). Do not recreate postgres — it is a protected stateful service with its volume intact.

---

## 5. Service Health Summary

| Service | HTTP /health | Docker health | Notes |
|---------|-------------|--------------|-------|
| arifOS MCP | ✅ 200 | healthy | 13 tools, `vault999_health: unreachable` expected |
| A-FORGE bridge | ✅ 200 | healthy | `af-bridge-prod` |
| GEOX EIC | ✅ 200 | none | `geox_eic` container, no Docker health check |
| WEALTH | ✅ 200 | none | `wealth-organ`, version 2026.04.29 |
| WELL | ✅ 200 | none | `well`, version 2026.04.29 |
| AAA A2A | ✅ 200 | running | `vault: DISCONNECTED` — vault999-writer missing |
| Hermes ASI | ✅ 200 | running | `hermes-agent`, role ASI 888_JUDGE |
| Postgres | ✅ | healthy | DB `vault999`, user `arifos_admin` |
| Caddy | ✅ | healthy | 80/443 public, valid config validated |

---

## 6. Remaining Drift Items (Non-Critical)

| Item | Severity | Action |
|------|----------|--------|
| `dozzle.arif-fazil.com` routes to `dozzle:8080` but container not running | LOW | Remove route or restore intentionally |
| `ollama.arif-fazil.com` routes to `ollama-engine-prod:11434` but not running | LOW | Remove route or restore intentionally |
| `vault999_health: unreachable` | EXPECTED | Start `vault999` + `vault999-writer` via 888_HOLD |
| `aaa-a2a` reports `vault: DISCONNECTED` | EXPECTED | Start `vault999-writer` via 888_HOLD |
| Postgres labeled for missing `/root/arifOS/docker-compose.yml` | LOW | Document ownership; don't recreate |
| `well` container attached to `bridge` network additionally | LOW | Later network cleanup only |

---

## 7. 888_HOLD Summary

| Command | Risk | Rollback | Status |
|---------|------|----------|--------|
| `docker exec caddy caddy reload --config /etc/caddy/Caddyfile` | Misdirection or brief outage | Restore Caddyfile + reload | Held |
| `docker compose -p compose -f /root/compose/docker-compose.yml up -d vault999 vault999-writer` | Schema write, exposure | Stop services | Held |
| Any `docker volume prune` | Irreversible data loss | N/A | Forbidden |
| Any `docker system prune -a` | Irreversible data loss | N/A | Forbidden |

---

**DITEMPA BUKAN DIBERI — 999 SEAL ALIVE**
